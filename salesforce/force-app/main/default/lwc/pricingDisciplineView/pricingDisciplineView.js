import { LightningElement } from 'lwc';

/**
 * Pricing Discipline view — thin wrapper around
 * <c-pricing-discipline-card>, which self-refreshes on
 * 'op:scan-completed'. No logic of its own.
 */
export default class PricingDisciplineView extends LightningElement {}
